% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-ordination.R
\name{axis_percent_var}
\alias{axis_percent_var}
\title{Percent of variation explained by each ordination axis}
\usage{
axis_percent_var(ord)
}
\arguments{
\item{ord}{An ordination object.}
}
\value{
\code{NULL}, or a list with \code{pct} (numeric, percentages, \code{NA} for
negative eigenvalues) and \code{note} (a character description of the
convention used).
}
\description{
Bray-Curtis and other semimetric dissimilarities are not always Euclidean, so
\code{ape::pcoa()} can return negative eigenvalues (whether it does depends on the
data). When it does, dividing by the sum of \emph{all} eigenvalues understates the
denominator, because the negatives subtract from it, and so overstates every
positive axis: on a test case with 5 of 29 eigenvalues negative, axis 1 reads
11.14\% that way against 9.30\% by the corrected convention. Both conventions
total 100\%; the difference is how the total is divided up.
}
\details{
This prefers the corrected relative eigenvalues that \code{ape} supplies exactly
when a correction is warranted (\code{Rel_corr_eig}), and otherwise restricts the
denominator to the positive eigenvalues. Where there are no negative
eigenvalues it returns the same numbers as the plain relative eigenvalues.
}
\keyword{internal}
