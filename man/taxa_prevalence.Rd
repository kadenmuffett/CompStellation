% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-core.R
\name{taxa_prevalence}
\alias{taxa_prevalence}
\title{Per-taxon prevalence within a set of samples}
\usage{
taxa_prevalence(
  physeq_sub,
  abundance_threshold = 0,
  abundance_type = c("counts", "relative")
)
}
\arguments{
\item{physeq_sub}{A phyloseq object already subset to a single group.}

\item{abundance_threshold}{The abundance a taxon must exceed in a sample to
count as present. Strictly greater than, so the default of 0 means
"any non-zero value".}

\item{abundance_type}{Either "counts" (the threshold is applied to the
values as supplied) or "relative" (each sample is converted to proportions
first, so the threshold is a relative abundance).}
}
\value{
A named numeric vector of prevalences in [0, 1], one per taxon.
}
\description{
Per-taxon prevalence within a set of samples
}
\keyword{internal}
